# lp 低代码开发平台 — 领域上下文

## 产品定位

面向不懂代码的小商家，以"进销存模板 + 字段微调"的方式自助搭建进销存系统。SaaS 多租户，手机号注册即用。

## 核心实体

| 实体 | 说明 |
|---|---|
| 商品（Product） | 库存中的 SKU，含名称、编码、规格、单位、分类、成本价、销售价等 |
| 供应商（Supplier） | 供货方，含名称、联系人、电话、地址 |
| 客户（Customer） | 购买方，含名称、联系人、电话、地址 |
| 仓库（Warehouse） | 存放商品的物理或虚拟仓库，库存按仓库独立 |
| 采购单（PurchaseOrder） | 向供应商采购的凭证，含明细、金额、状态 |
| 销售单（SalesOrder） | 向客户销售的凭证，含明细、金额、状态 |
| 库存（Inventory） | 商品在某个仓库的当前数量及金额 |
| 库存流水（InventoryLedger） | 每次出入库的明细记录，用于成本核算追溯 |
| 商户（Tenant） | SaaS 租户，一个商户 = 一个独立数据库 |
| 操作员（Operator） | 商户下的登录用户，关联角色 |

## 核心业务规则

- **成本核算**：移动加权平均（每次入库重算库存均价）
- **库存扣减**：出库时扣减对应仓库库存，同步记录流水
- **角色权限**：管理员（全部）/ 操作员（开单）/ 只读（查报表），菜单级
- **多仓库**：出入库必须指定仓库，报表支持按仓库筛选

## 术语约束

- "商品" 不是 "产品" 不是 "货物"——统一用"商品"
- "采购单" 不是 "进货单"
- "销售单" 不是 "出货单"
- "仓库" 不区分"总仓/分仓"概念，统一用"仓库"
- "商户" 不是 "租户" 不是 "公司"——对外文档用"商户"，代码中用 `tenant`
